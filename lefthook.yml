pre-commit:
  parallel: true
  commands:
    prettier:
      run: pnpm exec prettier --check .
    eslint:
      run: pnpm exec eslint .

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
