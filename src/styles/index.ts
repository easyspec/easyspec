/**
 * Emerald Theme - Main Export
 * Single dark theme based on DesignProposal5 and LandingDesign2
 */

// Export tokens explicitly
export { colors, typography, radius, shadows } from "./tokens";

// Export theme (no theme switching)
export { theme } from "./theme";
export { emeraldTheme } from "./theme";
